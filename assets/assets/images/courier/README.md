# Placeholder directory per gli sprite del corriere.
# Quando gli asset definitivi (PNG/sprite sheet) saranno pronti, aggiungere qui:
#   courier_normal.png   — sprite sheet (N frame) per stato normal
#   courier_boosted.png  — sprite sheet (N frame) per stato boosted
#
# Attualmente gli sprite sono generati programmaticamente in CourierSprite._buildVisuals().
# Per usare file PNG reali, sostituire _buildVisuals() con il caricamento da Flame.images.
